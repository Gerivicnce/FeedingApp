<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:FeedingApp.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewModels="clr-namespace:FeedingApp.ViewModels"
             x:Class="FeedingApp.Views.CalendarPage"
             x:DataType="viewModels:CalendarViewModel"
             Title="Naptár">

    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Dátum választó -->
        <DatePicker Grid.Row="0"
                    Date="{Binding SelectedDate}"
                    DateSelected="OnDateSelected" />

        <!-- Állat választó -->
        <Picker Grid.Row="1"
                Title="Válassz állatot"
                ItemsSource="{Binding Animals}"
                ItemDisplayBinding="{Binding Name}"
                SelectedItem="{Binding SelectedAnimal}" />

        <!-- Fotó készítése és etetés felvétele -->
        <HorizontalStackLayout Grid.Row="2" Spacing="8">
            <Button Text="📷 Fotó"
                    Clicked="OnTakePhotoClicked" />

            <Button Text="Új etetés"
                    Clicked="OnAddFeedingClicked" />
        </HorizontalStackLayout>

        <!-- Kamera előnézet / helyettesítő üzenet -->
        <ContentView x:Name="CameraContainer"
                     Grid.Row="3" />

        <!-- Az adott nap etetései -->
        <CollectionView Grid.Row="4"
                        ItemsSource="{Binding Events}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:FeedingEvent">
                    <StackLayout Padding="5">
                        <Label Text="{Binding FeedingTime, StringFormat='Etetés ideje: {0:yyyy.MM.dd HH:mm}'}" />
                        <Label Text="{Binding WeightGrams, StringFormat='Tömeg: {0} g'}" />
                        <Label Text="{Binding PhotoPath}" FontSize="10" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Opció: aktuális fotó elérési útja -->
        <Label Grid.Row="5"
               Text="{Binding CurrentPhotoPath}"
               FontSize="10" />
    </Grid>
</ContentPage>
